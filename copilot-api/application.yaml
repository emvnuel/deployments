apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: copilot-api
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/emvnuel/deployments.git
    targetRevision: HEAD
    path: copilot-api
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
