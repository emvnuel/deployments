apiVersion: apps/v1
kind: Deployment
metadata:
  name: yt-downloader
  namespace: default
spec:
  selector:
    matchLabels:
      app: yt-downloader
  template:
    metadata:
      labels:
        app: yt-downloader
    spec:
      containers:
      - name: yt-downloader
        image: myawesomeapps/yt-downloader:1.1-multiarch
        resources:
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: yt-downloader-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: yt-downloader
  ports:
  - port: 8080
    targetPort: 5000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yt-downloader-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
  labels:
    name: yt-downloader-ingress
spec:
  tls:
    - hosts:
      - "yt-downloader.labzzz.shop"
      secretName: "yt-downloader-tls"
  rules:
  - host: yt-downloader.labzzz.shop
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: yt-downloader-svc
            port: 
              number: 8080 